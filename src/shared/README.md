# Shared

Shared definitions used by all supported WoW versions.

The project currently uses a simple JSON layout format documented in
`docs/layout-coordinate-system.md`. A stricter schema can be added later once
the first version renderer stabilizes.

