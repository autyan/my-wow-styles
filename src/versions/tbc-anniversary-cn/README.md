# TBC Anniversary CN

Current and only supported version.

This version targets the WoW China TBC Anniversary client installed under the
local Proton/Steam prefix. Local paths are intentionally kept out of source
version data and should be stored in `profiles/local.json`.

